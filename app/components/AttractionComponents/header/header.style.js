import {StyleSheet} from "react-native";

const styles = StyleSheet.create({
    backButtonContainer : {
        position: 'absolute',
        top: 50,
        left: 20
    },
})

export default styles;
